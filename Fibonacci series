#include<stdio.h>
int fib(int);
int main()
{
	int n,f;
	//printf("enter the number \n");
	scanf("%d",&n);
	f=fib(n);
	printf("The term %d in the Fibonacci series is %d",n,f);	
}
int fib(int x)
{
	if(x==1)
	{
		return 0;
	}
	else if(x==2)
	{
		return 1;
	}
	else
	{
		return fib(x-1)+fib(x-2);
	}
}
