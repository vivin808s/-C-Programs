#include <stdio.h>
#include <string.h>
int main()
{
   char a[10];
  scanf("%s",a);
  int i;
  for(i=0;i<strlen(a);i+=2)
  {
    if(i+1<strlen(a))
    {
      char temp =a[i];
      a[i]=a[i+1];
      a[i+1]=temp;
    }
    
  }
  printf("%s\n",a);
}
