#include <stdio.h>
int main() {
 int t;
 scanf("%d", &t);
 while (t-- > 0) {
 int num;
 scanf("%d", &num);
 int r, count = 0, ans = 0;
 while (num != 0) {
 r = num % 10;
 
 if (r == 0) {
 count++;
 } else {
 ans = (ans * 10) + r;
 }
 num = num / 10;
 }
 printf("%d", ans);
 for (int i = 0; i < count; i++) {
 printf("0");
 }
 printf("\n");
 }
 return 0;
}
