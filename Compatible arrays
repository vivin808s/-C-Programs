#include <iostream>
using namespace std;

int main() 
{
int n,m,i,c,d=0;
  scanf("%d",&n);
  int a[n];
  for(i=0;i<n;i++)
    scanf("%d",&a[i]);
  scanf("%d",&m);
  int b[m];
  for(i=0;i<m;i++)
    scanf("%d",&b[i]);
  
  c=n>m?n:m;
  
  for(i=0;i<c;i++)
  {
    if(a[i]<b[i])
    {
      printf("Incompatible");
      d=0;
      break;
    }
    else
    {
       d=1;
    }
  }
  if(d==1)
  printf("Compatible");

  
}
